/*
 * BugPage Messages
 *
 * This contains all the text for the BugPage component.
 */
import { defineMessages } from 'react-intl';

export const scope = 'boilerplate.containers.BugPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'Bugs',
  },

  helloWorld: {
    id: `${scope}.helloWorld`,
    defaultMessage: 'Hello World',
  }


});
